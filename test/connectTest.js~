var simple = require('../index');
var options = require('../lib/config.json');
var db = simple.createConnect(options);

    db.connect(function(err, connect){

        if(err){
            throw err;
        }
        console.log(connect.id);
        db.release();
    });
